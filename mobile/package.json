{
  "name": "skatehubba-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "SkateHubba mobile app — Expo + React Native with S.K.A.T.E. battle, spot mapping, and leaderboard.",
  "scripts": {
    "start": "expo start",
    "dev": "expo start --dev-client",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "e2e:build:ios": "detox build --configuration ios.sim.debug",
    "e2e:test:ios": "detox test --configuration ios.sim.debug",
    "e2e:build:android": "detox build --configuration android.emu.debug",
    "e2e:test:android": "detox test --configuration android.emu.debug",
    "e2e:smoke": "detox test --configuration ios.sim.debug --testNamePattern \"Smoke\""
  },
  "dependencies": {
    "@skatehubba/types": "workspace:*"
  },
  "devDependencies": {
    "detox": "^20.30.0",
    "jest": "^29.7.0"
  }
}
