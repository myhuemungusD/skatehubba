// Delegates to the root-level script so that Vercel can find it when the
// build command ("node scripts/verify-public-env.mjs") is executed from the
// client/ working directory.
import '../../scripts/verify-public-env.mjs';
